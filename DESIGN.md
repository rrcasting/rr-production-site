# DESIGN.md — Rr.production's 版（ブループリント・スタイル）

作成: 2026-09-19。元は Refero Styles「Index」の DESIGN.md（Richard が選定）。
**御社用に3点だけ改造した**：①写真を主役として残す ②唯一のアクセント色を薄紫→金 `#C9A84C` ③有料フォント Raveo → Google Fonts の Inter / Noto Sans JP。
**このファイルが唯一の色・フォント・余白・部品の基準。** Claude Code はここに無い値を使わない。

---

## 世界観（1段落）

黒い製図台に置かれた設計図。画面全体が「線で描いた下書き」に見える：ほぼ黒の地に **点線の枠**、極太の見出し、等幅フォントの小さなラベル。色は99%無彩色、**金は注釈用の1本のペン**としてだけ使う。影・グラデーション・光沢は使わない。段差は「黒→炭色」の面の差と点線の枠だけで表す。**その設計図の中に、キャストの実写真を「貼り込んだ写真」として置く**——写真は加工しない、色を被せない、切り抜かない。

---

## 色

| 名前 | 値 | 用途 |
|---|---|---|
| Void | `#080808` | ページ背景（現行ブランドの黒をそのまま使う） |
| Carbon | `#1c1c1c` | 一段上の面：カード・セクションの地・ヒーローの中心 |
| Graphite | `#4d4d4d` | 三次的な罫線・無効状態の枠 |
| Steel | `#808080` | 二次テキスト・メタ情報・小さな区切り線 |
| Ash | `#ababab` | 補助テキスト・キャプション |
| Paper | `#F0EDE6` | 主テキスト・ロゴ・ボタンの枠線・チップ |
| **Gold** | **`#C9A84C`** | **唯一の有彩色。** チェック印・小さなアイコン・ラベルの1語・点線の枠の「一部」・写真の角の注釈ラベル。**塗りつぶしボタン・大きな面・本文には使わない** |

禁止：緑・赤・青・紫・グラデーション。（現行の WhatsApp ボタンの緑 `#22c55e` も**廃止**。WhatsApp ボタンも白枠ピルにし、アイコンだけ白。クラス名 `.btn-wa-main` / `.btn-wa-hero` は残してよい）

---

## フォント（Google Fonts・追加は下の3つまで）

| 役割 | フォント | 太さ | 備考 |
|---|---|---|---|
| 見出し（EN/ES） | **Inter** | **900** | 元の Raveo 1000 の代替。**見出しは必ず 900。** 700 以下は別のデザインに見える |
| 見出し（JA） | **Noto Sans JP** | **900** | 日本語は Inter で出ないので必ずこちら |
| 本文（3言語） | Inter / Noto Sans JP | 400・500 | |
| 小ラベル・チップ・番号 | **Geist Mono**（Google Fonts。無ければ JetBrains Mono） | 500 | 大文字・字間 +0.02em。「01 / 02 / 03」「REGISTER」「TOKYO · FILM · TV」など |

現行の Playfair Display は**使わない**（読み込みも外す）。

### 文字サイズ（この7段だけ。中間サイズを足さない）

| 役割 | サイズ | 行間 | 字間 |
|---|---|---|---|
| micro（チップ） | 9px（スマホは 10px） | 1.6 | +0.02em |
| caption | 12px | 1.2 | 0 |
| body-sm | 14px | 1.6 | -0.01em |
| body | 16px | 1.6 | -0.01em |
| subheading | 24px | 1.4 | -0.01em |
| heading | 32px | 1.2 | -0.01em |
| display | **70px（PC）／ 40px（390px幅）** | 1.1 | **-0.04em** |

JA の display は 40px（PC）／ 28px（スマホ）。日本語は字間 0。

---

## 余白・形

- ページ最大幅 1200px。セクション間 80px（スマホ 48px）。カード内 24px。要素間 10px
- 角丸：ボタン・チップ **100px（完全なピル）**／写真タイル・パネル **20px**／アイコン丸 **50px**／ナビの塗り要素 6px
- **影・ぼかし・光沢：全面禁止**

---

## 部品

### 1. 点線セクション枠（このデザインの核）
すべてのセクションを **1px dashed** の枠で囲む。Carbon の地の上では `#080808`、Void の地の上では `#1c1c1c`（黒地に薄い点線）。**実線で囲まない。** 点線が「設計図の枠」。
セクション内の上左に **Geist Mono の小ラベル**（例 `01 — FOR PRODUCTIONS`）を置く。

### 2. ヒーロー
Carbon の地に、中心が `#1c1c1c`・端が `#080808` の**放射状の薄い明るさ**（製図台のライト。これだけが唯一の「柔らかさ」）。中央揃えの縦積み、最大幅 720px：
- チップ（点線の白枠ピル・Geist Mono 9px・大文字）例 `TOKYO · FILM · TV · CM`
- 見出し 70px / 900（現行の文章をそのまま）
- 本文 16px Steel
- **ボタン2本（白枠ピル）**：「制作会社の方 → WhatsApp」「出演したい方 → 登録フォーム」
- **その直下に、キャスト写真の「コンタクトシート」**：点線枠の帯に写真 6〜8 枚を横並び（スマホは横スクロール可）。各写真は 20px 角丸・Carbon の地・**加工なし**。角に Geist Mono の小ラベル（`CAST 01` 等・白）を置いてよい

### 3. ボタン（1種類だけ）
白 1px 実線の枠・塗りなし・100px 角丸・padding 8px 18px（スマホは 12px 20px でタップ域確保）・14px 500 白。hover は枠が Gold になる。**塗りつぶしボタンは作らない。** 主・副の区別は「文字の左の小アイコン」だけ。

### 4. チップ（eyebrow）
点線 1px の白枠・100px 角丸・padding 4px 12px・Geist Mono 9px 大文字。末尾に 10px の丸の中に矢印を置いてよい。

### 5. ステップ（登録のしかた・進め方）
3〜4 列。枠なし・影なし。各列：50px 角丸の Carbon の丸の中に Gold の線アイコン → 16px 500 の見出し → 14px Steel の説明。列の間は 24px。**列の左上に Geist Mono で `01` `02` `03`。** 全体を点線枠で囲む。

### 6. 特徴パネル（2列：文＋写真）
左：12px 大文字 Geist Mono のラベル（Steel）→ 32px / 900 の見出し → 16px Ash の本文 → **Gold のチェック印**付きリスト（14px 白・間隔 10px）。
右：**写真 1 枚**を 1:1 または 4:5 の 20px 角丸タイルに（Carbon の地・object-fit: cover・**顔を切らない**）。元の DESIGN.md はここに線画イラストを置くが、御社では**写真**を置く。
スマホでは写真が上・文が下。本文は**左揃え**（中央揃えはヒーローだけ）。

### 7. ギャラリー（16枚）
点線枠の中に 2〜4 列のグリッド、隙間 10px、各 20px 角丸。**モノクロ化・色被せ・ぼかし禁止。** hover で 1px の Gold 枠が出るだけ。

### 8. ナビ
ヒーロー上は透明、スクロールで `#080808` の地＋1px `#1c1c1c` の下線。左にロゴ（白・Inter 500）、右に言語切替（`ENGLISH / ESPAÑOL / 日本語`・**`data-code` 方式のまま**）とボタン1本（登録フォーム）。既存の `#lang-bar` と `#sticky-cta` は**そのまま残す**（見た目だけこの規則に合わせる）。

### 9. FAQ
点線の罫（`#1c1c1c`）で区切った折りたたみ。質問は 16px 500 白、答えは 14px Ash。開閉の印は「+ / −」を Geist Mono で。

### 10. フッター
Void の地・上に点線 1px。ロゴ・リンク・SNS はテキストのみ（12px Steel）。

---

## やる／やらない

**やる**
- 見出しは全部 900。display は 70px / 行間 1.1 / 字間 -0.04em
- すべてのセクションを点線枠で囲む
- Gold は「注釈」にだけ（チェック・小アイコン・hover の枠・小ラベルの1語）
- ボタンは白枠ピル 1 種類
- 面の段差は Void → Carbon の 2 段だけ
- 写真は無加工・角丸 20px・顔を切らない

**やらない**
- 影・グラデーション（ヒーローの放射光だけ例外）・ぼかし・光沢
- 実線でセクションを囲む
- Gold の塗りつぶしボタン・Gold の本文
- 中間サイズ（18px・28px 等）
- 本文の中央揃え
- 写真のモノクロ化・色被せ・トリミングで顔が切れること
- 線画イラスト・アイコンの大量配置（元の DESIGN.md にある「歯車・箱・パイプライン」は御社には**不要**。置くなら写真）

---

## CSS 変数（そのまま貼る）

```css
:root{
  --void:#080808; --carbon:#1c1c1c; --graphite:#4d4d4d; --steel:#808080; --ash:#ababab; --paper:#F0EDE6;
  --gold:#C9A84C;
  --font-sans:'Inter','Noto Sans JP',ui-sans-serif,system-ui,sans-serif;
  --font-jp:'Noto Sans JP','Inter',sans-serif;
  --font-mono:'Geist Mono','JetBrains Mono',ui-monospace,Menlo,monospace;
  --t-micro:9px; --t-caption:12px; --t-body-sm:14px; --t-body:16px; --t-sub:24px; --t-h:32px; --t-display:70px;
  --w-regular:400; --w-medium:500; --w-black:900;
  --s-4:4px; --s-8:8px; --s-10:10px; --s-12:12px; --s-16:16px; --s-24:24px; --s-48:48px; --s-80:80px;
  --max:1200px; --gap-section:80px; --pad-card:24px; --gap:10px;
  --r-pill:100px; --r-tile:20px; --r-icon:50px; --r-nav:6px;
  --dash-on-carbon:1px dashed #080808; --dash-on-void:1px dashed #1c1c1c;
}
@media (max-width:480px){ :root{ --t-display:40px; --t-micro:10px; --gap-section:48px; } }
```